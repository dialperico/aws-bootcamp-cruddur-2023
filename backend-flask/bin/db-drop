#! /usr/bin/bash

echo "executing dp-drop proccess"

NO_DB_CONNECTION_URL=$(sed 's/\/cruddur//g' <<<"$CONNECTION_URL")

psql $NO_CONNECTION_URL -c "\l"
#psql $NO_CONNECTION_URL -c "DROP DATABASE cruddur;"
